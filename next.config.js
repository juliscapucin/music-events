/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "images.unsplash.com",
      "musicevents.wordpresssites.host",
      "next.config.js",
    ],
  },
};

module.exports = nextConfig;
